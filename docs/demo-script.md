# Five-to-seven-minute demonstration

1. **Problem, 30 seconds:** Laboratory observations currently need repeated transcription, calculations and report assembly. We digitize that path for SIH26035. Show the Type Evaluation Test Report objective.
2. **Tester, 60 seconds:** Log in, register fictional DEMO-30, show Max/Min/e/d/class, and create the scoped plan. Point out three automated tests and the remaining coverage catalogue.
3. **Explainable arithmetic, 90 seconds:** At L=10000 g, I=10000 g, e=10 g, deltaL=3 g: E=2 g. Linked zero observation gives E0=1 g, so Ec=1 g. MPE=10 g; PASS. Show the clause and band. Demonstrate a labelled synthetic entry error I=10020 g giving Ec=21 g and FAIL. Correct this demo transcription with a reason/audit entry; never edit real failed readings just to get PASS.
4. **Other procedures, 45 seconds:** Show four eccentric segments and two repeatability series of ten. Explain that repeatability checks each error and the spread.
5. **Independent review, 60 seconds:** Submit. In another browser profile the approver sees the frozen snapshot, references and scope. Approve the report for issue. Approval cannot rewrite a result.
6. **Report, 45 seconds:** Generate and retrieve the private stored PDF, then show version and audit history.
7. **Scope, 20 seconds:** These are synthetic observations and selected tests. Full standard conformity is not determined. DOCX, remaining R76 tests and the validated Indian legal layer remain future work if not implemented.

Keep one prefilled, clearly labelled fictional evaluation as recovery, plus a downloaded PDF and short recording of the genuinely completed deployed workflow. Do not present either fallback as a live successful action if the hosted system fails.
