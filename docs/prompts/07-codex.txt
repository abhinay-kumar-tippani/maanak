Read docs/AI_WORK_RULES.md, docs/master-blueprint.md, docs/security-contract.md, docs/rule-registry.json, and src/contracts/domain.ts first. Preserve existing generated AGENTS.md instructions and the installed Next.js major version.
Change only the allowed paths below. Do not modify other files, standards PDFs, source hashes, fixtures, formulas, types, auth, migrations, or package versions unless explicitly allowed. If a dependency or interface change is genuinely required, explain the exact reason and affected files before that change; do not silently rewrite the repository. Never weaken RLS, omit version checks, manufacture successful results, trust browser PASS/FAIL, or use an LLM for runtime compliance.
Use the existing ActionResult<T> and domain DTOs. Raw numeric metrology inputs travel as decimal strings with explicit units. Use Zod at boundaries and Decimal arithmetic in pure rule code. Never put privileged clients or runtime calculators in a client module. No source means NOT VERIFIED FROM PROVIDED AUTHORITATIVE SOURCE and no automated decision.
After editing, list changed files, explain the data path, run the stage-specific checks and report actual output. Do not claim a check was run when it was not. No git push, deployment, role change, destructive migration, or account message unless the current stage explicitly instructs it. Do not add a guessed helper that bypasses a missing prerequisite.

STAGE 7: Authorization, transaction and state guards
ALLOWED PATHS: vitest.config.ts; tests/setup.ts; src/server/auth/authorize.ts; src/server/db/commands.ts; src/server/workflow/*; supabase/migrations/202609090003_command_guards.sql; tests/workflow/*; tests/security/*
TASK: Implement requireActor, requireRole/assignment, a server-only privileged client, ActionResult error mapping, evaluation state graph and database guard. All command RPCs require actor validation, evaluation FOR UPDATE, expected versions and local actor context. Revoke public RPC EXECUTE. Add state transition unit tests and integration test harness using distinct test users; no feature RPC stubs returning fake success.
INPUTS: The referenced source PDFs, verified registry, current repository and relevant existing DTOs/actions. Use only data available at this stage.
OUTPUTS: Build the command boundary and state guard before CRUD.
ACCEPTANCE: Prove tester cannot promote own role; admin is not approver; invalid transitions throw; closed evaluations cannot accept edits.
STOP CONDITION: Command boundary exists; deny tests pass against the test backend; state guard migration applied.
