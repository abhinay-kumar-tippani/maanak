import {NextResponse}from"next/server";import{issueApprovedReport}from"@/server/reports/review";export const runtime="nodejs";export async function POST(request:Request,{params}:{params:Promise<{evaluationId:string}>}){const{evaluationId}=await params;let body:{expectedEvaluationVersion?:number}={};try{body=await request.json();}catch{return NextResponse.json({ok:false,error:{code:"VALIDATION_ERROR",message:"A JSON version payload is required."}},{status:400});}const result=await issueApprovedReport(evaluationId,Number(body.expectedEvaluationVersion));return NextResponse.json(result,{status:result.ok?200:400});}
