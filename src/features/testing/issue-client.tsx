"use client";import{useState,useTransition}from"react";import{useRouter}from"next/navigation";export function IssueButton({evaluationId,version}:{evaluationId:string;version:number}){const[pending,start]=useTransition();const[msg,setMsg]=useState("");const router=useRouter();return <><button disabled={pending} onClick={()=>start(async()=>{const response=await fetch(`/api/reports/${evaluationId}/issue`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({expectedEvaluationVersion:version})});const r=await response.json();setMsg(r.ok?"PDF issued to private storage.":r.error?.message??"Issuance failed.");if(r.ok)router.refresh();})} className="rounded bg-teal-700 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{pending?"Generating PDF…":"Generate and issue final PDF"}</button><span role="status" className="ml-3 text-sm">{msg}</span></>}
